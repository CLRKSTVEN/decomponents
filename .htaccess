Options -Indexes
RewriteEngine On
RewriteBase /decomponents/

# Send everything (including directory requests) to the CI3 front controller
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.*)$ index.php/$1 [L]
